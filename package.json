{
  "name": "openpoke-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "imessage-service"
  ],
  "description": "OpenPoke monorepo with iMessage service",
  "scripts": {
    "build:service": "cd imessage-service && npm run build",
    "build": "npm run build:service"
  }
}

